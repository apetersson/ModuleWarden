"""ModuleWarden Python fine-tune sibling.

Companion to ``finetune/`` that emits records matching the canonical
schemas in ``finetune/contracts/`` and drives the H100 training plus
4-arm eval matrix described in ``finetune/README.md``.
"""

__version__ = "0.1.0"
